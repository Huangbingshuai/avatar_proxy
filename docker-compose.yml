services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - avatar-proxy-data:/app/data
    restart: unless-stopped

volumes:
  avatar-proxy-data:
