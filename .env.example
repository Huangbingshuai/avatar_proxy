# 仅供视频调试和接入示例使用的客户业务 API 地址。
# 管理员认证始终访问同源 /api/internal/*，不使用此变量。
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# 仅供本地 `npm run dev` 使用；Vite 将同源 /api 请求代理到此后端。
CONSOLE_API_PROXY_TARGET=http://127.0.0.1:8000
